<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodfightersnew">

    <application
        android:allowBackup="true"
        android:icon="@drawable/foodfighters"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodFightersNew">
        <activity android:name=".Enter_items"></activity>
        <activity android:name=".donate" />
        <activity android:name=".datails" />
        <activity android:name=".signup" />
        <activity android:name=".start" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".MainActivity2"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>